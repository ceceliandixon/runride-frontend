import { Box } from "@mui/material";
import { styled } from "@mui/system";

// Component to apply flex spacing throughout project

const FlexBetween = styled(Box) ({
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center"
})

export default FlexBetween;
